/**
 * 自动模板导入已整体禁用。
 * 如需恢复，将本文件恢复到先前实现即可。
 */

export async function importNewHighQualityTemplates(): Promise<void> {
  // disabled
}

export async function checkAndImportTemplates(): Promise<void> {
  // disabled
}