/**
 * Hook-specific types
 * Re-exports from the main types file for convenience
 */

export type {
  // Hook return types
  UseApiConfigReturn,
  UseSystemSettingsReturn,
  UseNotificationReturn,
  
  // System settings
  SystemSettings,
  
  // Utility types
  DeepPartial,
  RequiredKeys,
  OptionalKeys,
  KeyOf,
  ValueOf,
} from './index';