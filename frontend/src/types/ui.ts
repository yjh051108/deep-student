/**
 * UI-specific types
 * Re-exports from the main types file for convenience
 */

export type {
  // UI component types
  NotificationMessage,
  StreamChunk,
  StreamMessage,
  StreamOptions,
  
  // Component props
  BaseComponentProps,
  ModalProps,
  FormProps,
  ListProps,
  PaginationProps,
  
  // File management
  ImageFile,
  FileUploadResult,
  
  // Error handling
  AppError,
  AsyncResult,
  
  // Theme
  ThemeColors,
  Theme,
  
  // Events
  CustomEvent,
  EventHandler,
} from './index';